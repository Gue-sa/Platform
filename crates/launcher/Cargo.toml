[package]
name = "launcher"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "3.1.1"
dialoguer = "0.12.0"
getset = "0.1.6"
serde = { version = "1.0.228", features = ["derive"]}
shared = { path = "../shared" }
