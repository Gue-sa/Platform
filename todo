* Refactor ce qui a été marqué
* Implémenter des Result partout où on peut
* Vérifier que les Result gèrent bien les erreurs
* Remplacer les .unwrap() et !panic() par des ? et autres gestions d'erreurs correctes, ajouter des Result<(), &'static str>s
* Remplacer les threads par du tokio
* condvar + clock thread
* refactor le systeme de messages qui est DEGUEULASSE
* changement de rr
* mettre les fields en privé
* mettre les si en usize quand nécessaire
* implémenter partout où c'est possible Display, Debug, Index et IndexMut